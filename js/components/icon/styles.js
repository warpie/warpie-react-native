import React from 'react-native';
import {Colors} from '../../theme';
var styles = React.StyleSheet.create({
  container: {},
  text : {color: Colors.brandRed,fontSize:14,fontWeight:'bold',textAlign:'right',marginBottom:20}
});

export default styles;
